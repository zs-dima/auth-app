name: json_converter

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.3"

dependencies:
  # SDK extensions
  collection: any
  meta: any

  # Code generation
  json_annotation: ^4.8.1

dev_dependencies:
  lints_tool:
    path: ../../../tool/lints_tool
