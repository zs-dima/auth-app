name: json_converter

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.1.2"

dependencies:
  # SDK extensions
  collection: any
  meta: any

  # Code generation
  json_annotation: ^4.8.1

dev_dependencies:
  # Analyzer extensions
  dart_code_metrics: ^5.7.0
