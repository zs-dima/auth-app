name: grpc_model
resolution: workspace

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  core_model:
    path: ../../model/core_model

  # SDK extensions
  async: any
  collection: any
  meta: any
  uuid: any
  fixnum: any

  # API
  grpc: ^5.1.0
  protobuf: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  lints_tool:
    path: ../../tool/lints_tool
