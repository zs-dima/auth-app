name: core_model
resolution: workspace

description: Core data classes

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  # SDK extensions
  collection: any
  meta: any

dev_dependencies:
  lints_tool:
    path: ../../tool/lints_tool
